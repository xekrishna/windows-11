import { edge } from "@/public/icons";
import Image from "next/image";

const Chrome = () => {
  return (
    <div>
      <Image src={edge} alt="edge" className="h-8 w-8 p-[0.07rem]" />
    </div>
  );
};

export default Chrome;
